# APPENDIX — Hypotheses (harvested from all 16 domain reports)

Consolidation pass 2026-09-13. Every HYPOTHESES section from `domains/*.md`, verbatim-condensed (long entries truncated with `…`; full text in the source report). 81 hypotheses total. Confirmed-since-writing: DATA H1 ((default) DB holds stale PHI + refreshTokens) was confirmed by COST-006's live read; DX H1 (no branch protection) and TEST's deploy-gating hypothesis were live-confirmed by the skeptic pass (CICD-001).

| id | hypothesis | confirmation method | source report |
|---|---|---|---|
| H-ARCH-1 | H1: Cross-document invariants outside the 7 transactional sites can race. E.g. a composite-meal entry write plus its day rollup, or program-apply plus materialized phase docs, may interleave with a concurrent client. | Confirm: read `core/nutrition/NutritionService.java` write paths and `persistence/nutrition/FirestoreNutritionDailyLogRepository.java` to see whether rollup totals are recomputed inside the same transaction as entry writes, and whether `WorkoutProgramService` apply is batched (`WriteBatch`) vs sequential. [Guessing] | domains/01-architecture.md |
| H-ARCH-2 | H2: Parked parent CREATE + surviving child mutations can wedge a subtree. Outbox drains in global seq order, but a terminal-4xx park of a parent create (e.g. a nutrition day/entry composite) is skipped by automatic drains while later child ops for the same entity family may still replay against a parent the server never accepted. | Confirm: read `OutboxRepository.drain` (lines ~140-220) and `OutboxReducer.kt` for whether parked-entity descendants are also held back; write a unit test with a parked CREATE and a queued UPDATE for a child id sharing the `"<parent>/<child>"` prefix. [Guessing] | domains/01-architecture.md |
| H-ARCH-3 | H3: web type drift already exists in optional-field handling. `web/lib/types/nutrition.ts` marks `analysisStatus`/`ingredients`/`date` optional while the Android domain gives them defaults; if `EntryResponse` always serializes them, the web types understate the contract (harmless) — but if any field the web marks required (`servingGrams: number`) is nullable server-side (`FoodE … | Confirm: diff `EntryResponse.java` field nullability against `web/lib/types/nutrition.ts:19-46` and grep web for unguarded `entry.servingGrams` arithmetic. [Likely] | domains/01-architecture.md |
| H-ARCH-4 | H4: The layering exemption for root packages is doing hidden load-bearing work. `auth`, `push`, `admin`, `platform` may import `core` and each other freely; if `push` or `platform/webhook` ever import `api` DTOs it would invert the api→core direction invisibly since root packages have no stated rules (ARCH-008). | Confirm: `grep -rn 'import com.gte619n.healthfitness.api' backend/src/main/java/com/gte619n/healthfitness/{auth,push,admin,platform,jobs,config}` (was not run exhaustively for `api` imports this audit — only `persistence`). [Guessing] --- | domains/01-architecture.md |
| H-SEC-1 | H1: Firebase Android API key restrictions. `android/app/google-services.json` ships an `AIza…` key (39 chars, normal for Firebase clients). Whether it is restricted to the Android package + SHA fingerprints is not determinable from the repo. | Confirm: GCP Console → APIs & Credentials → key restrictions, in project health-fitness-160. If unrestricted, restrict to Android app + required APIs. | domains/02-security.md |
| H-SEC-2 | H2: Cloud Run logs actually retain the Withings `secret` query param (SEC-004 premise). Default Cloud Run request logs include full `requestUrl`. | Confirm: ADC Logging API: query `resource.type=cloud_run_revision AND httpRequest.requestUrl:"/api/webhooks/withings"` and inspect whether the query string is present. | domains/02-security.md |
| H-SEC-3 | H3: `CORS_ALLOWED_ORIGINS` prod value. Not visible in `backend/cloudbuild.yaml` excerpt reviewed; if unset, CORS fails closed (breaks web, so presumably set). | Confirm: `gcloud run services describe` env block for the backend service. | domains/02-security.md |
| H-SEC-4 | H4: Wear backup default. No `allowBackup` attribute found in `android/wear/src/main/AndroidManifest.xml`, implying platform-default backup (enabled) for `hf-wear-auth`. | Confirm: the merged manifest from a wear build output (`processDebugManifest` artifact) — read-only from a past CI artifact, or accept SEC-005's fix which makes it explicit either way. | domains/02-security.md |
| H-SEC-5 | H5: Spring relaxed-binding source for `APP_AUTH_DEV_LOGIN_ENABLED`. UAT proves the env var binds; whether any *other* property source (e.g. a stray Cloud Run env from an old revision) could set it in prod is a deployment-state question. Confirm alongside H3 in the same `services describe`. | (method embedded in hypothesis text) | domains/02-security.md |
| H-SUP-1 | CVE-2026-59889 (jackson-databind @JsonView/@JsonUnwrapped bypass, moderate, 2026-07-21): patched version not confirmed; 2.21.4 (2026-06) *may* predate the fix. Verify GHSA-5gvw-p9qm-jgwh's patched range next backend touch. | (method embedded in hypothesis text) | domains/03-supply-chain.md |
| H-SUP-2 | micrometer 1.15.12 and httpcore5 5.4.3 pins were not re-verified against advisories newer than their comments. | (method embedded in hypothesis text) | domains/03-supply-chain.md |
| H-SUP-3 | Whether any of netty 4.1.138's 23 CVEs are reachable in this backend (netty arrives via gRPC clients — Firestore/GenAI — not as a server) — bump regardless; Trivy doesn't do reachability either. | (method embedded in hypothesis text) | domains/03-supply-chain.md |
| H-SUP-4 | Android transitive closure (and thus any copyleft hiding in transitives, e.g. via mikepenz renderer) was not resolved — R3 forbade running gradle. A one-time `licensee`/AboutLibraries run would settle it. | (method embedded in hypothesis text) | domains/03-supply-chain.md |
| H-SUP-5 | `com.mikepenz:multiplatform-markdown-renderer-m3` assumed Apache-2.0 (not fetched). | (method embedded in hypothesis text) | domains/03-supply-chain.md |
| H-SUP-6 | functions deploy mechanism (assumed manual gcloud; no pipeline found) — | confirm: operator. | domains/03-supply-chain.md |
| H-PERF-1 | H1 — Sync page latency at 2-year account age is >5 s. | Measure: seed 730 `nutritionDays` day-docs (+1 entry each) for a test uid in the dev database; time `GET /api/me/sync?limit=500` cold and with a fresh cursor; count RPCs via gRPC client logging. (Backs PERF-001 magnitude.) | domains/04-performance.md |
| H-PERF-2 | H2 — End-to-end first-morning dashboard is ~18–20 s. | Measure: synthetic check (Cloud Scheduler + curl with `-w %{time_total}`) at 06:00 after idle night against `app.tesseta.com`, capture web+backend `container/startup_latencies` correlation for the same minute. | domains/04-performance.md |
| H-PERF-3 | H3 — gh-refresh per-user wall time is 2–5 s. | Measure: Cloud Logging (ADC REST) query on the gh-refresh job's execution logs: total duration ÷ connected users; today N=1 so instrument a per-user timing log line first. | domains/04-performance.md |
| H-PERF-4 | H4 — Capture endpoint heap high-water ~3–4× image size per request. | Measure: enable `-XX:NativeMemoryTracking=summary`/heap histogram or a simple `Runtime.totalMemory` log around `analyzeMeal` with a 20 MB test photo. | domains/04-performance.md |
| H-PERF-5 | H5 — Warm backend p50 excluding cold starts is well under 1.34 s (i.e., the measured p50 is startup-polluted). | Measure: re-query `request_latencies` grouped by `response_code_class` with alignment 1h and correlate spikes with `startup_latencies` points; or filter out the first request per instance via log-based metric on `instanceId`. | domains/04-performance.md |
| H-PERF-6 | H6 — Gemini p95 for meal analysis dominates capture UX (>8 s). | Measure: add a timer metric around `generateContent` in MealPhotoExtractor; one week of data. --- | domains/04-performance.md |
| H-DATA-1 | H1 [Guessing]: The `(default)` database still holds a stale pre-cutover copy of user health data (the copy scripts imply it was the original home). | Confirm: REST GET `…/databases/(default)` then a collection list; if populated, it is an unmanaged second copy that deletion (DATA-003) and backups (DATA-001) both ignore. | domains/05-data-lifecycle.md |
| H-DATA-2 | H2 [Guessing]: Old Firestore export snapshots persist in `gs://health-fitness-160-firestore-exports` with no lifecycle rule, containing full health-data copies. | Confirm: `gcloud storage ls -r gs://health-fitness-160-firestore-exports` (read-only). | domains/05-data-lifecycle.md |
| H-DATA-3 | H3 [Likely]: WORKOUT_SCHEDULED parked outbox rows (`PARKED_NEXT_ATTEMPT = Long.MAX_VALUE`, `OutboxRepository.kt:283`) can accumulate forever if the bespoke restore flow is never triggered. | Confirm: trace `rearmFailed()` call sites in the Android UI layer. | domains/05-data-lifecycle.md |
| H-DATA-4 | H4 [Guessing]: The public nutrition-photos bucket serves images under guessable object names, making "deleted" meals fetchable post-deletion. | Confirm: read the upload path in the backend nutrition image service for object-name construction (UUID vs derived). | domains/05-data-lifecycle.md |
| H-XPLAT-1 | H1: Backend `TodaysDosesService` lacks android's MONTHLY day-of-month clamp (`ReminderPlanner.kt:34-36`) or WEEKLY empty-days semantics — I only read the CYCLE/PRN arms of the backend switch. | Verify: read `TodaysDosesService.java:90-128` in full and diff each `FrequencyType` arm against `ReminderPlanner.isDueOn`. | domains/06-cross-platform.md |
| H-XPLAT-2 | H2: Prod backend JVM default zone is UTC (asserted by `TimeZoneInterceptor.kt:9` and `TimezoneCookie.tsx:7-8` comments). | Verify: Cloud Run service env/`-Duser.timezone` in `infra/` or the deploy `cloudbuild.yaml`. | domains/06-cross-platform.md |
| H-XPLAT-3 | H3: Web GET of a day whose entries include an `adjustment` object renders fine (extra JSON key ignored) — inferred from JS semantics, not tested. | Verify: fixture render test of `NutritionMeals` with an EntryResponse-shaped payload. | domains/06-cross-platform.md |
| H-XPLAT-4 | H4: Composite entries with calorie-only ingredients actually occur in prod data (drives XPLAT-004 severity). Verify (privacy-safe): count, in the prod `production` Firestore, composite entries having any ingredient with all-null macro grams — aggregate count only, no content. | (method embedded in hypothesis text) | domains/06-cross-platform.md |
| H-XPLAT-5 | H5: The outbox replay for a composite portion PATCH sends only `quantity` (no macros), letting the server recompute authoritatively — the local recompute is display-only until pull. | Verify: `OutboxEndpointRegistry.kt` + `OutboxReducer.kt` payload shape for `nutrition entries PATCH`. | domains/06-cross-platform.md |
| H-UX-1 | H1 — Back press mid-workout is probably safe but unconfirmed. `WorkoutSessionScreen.kt` has no `BackHandler` (verified: zero matches), so system back exits without the "Discard workout?" dialog (`:363-371`). However the draft is Room-persisted (ADR-0012 note in `WorkoutSessionViewModel.kt:45-46`) and a foreground service runs, so the session is *likely* resumable, not destroyed … | (method embedded in hypothesis text) | domains/07-ui-ux.md |
| H-UX-2 | H2 — Dashboard "last updated" header may mask per-card staleness (`PhoneHeader` in PhoneTodayScreen) — if it shows app-refresh time rather than per-source sync time, it worsens UX-005. | (method embedded in hypothesis text) | domains/07-ui-ux.md |
| H-UX-3 | H3 — Web dashboard `summary === null` for new users: whether a genuinely-empty account produces null (error copy shown) or a zeroed summary depends on backend response shape, not verifiable from web code alone (UX-011 tagged Likely for this reason). | (method embedded in hypothesis text) | domains/07-ui-ux.md |
| H-UX-4 | H4 — Wear: the wear module is a signed-in stub (`MainActivity.kt:37-43`); assumed intentionally pre-MVP, so its absent states were not raised as findings. | (method embedded in hypothesis text) | domains/07-ui-ux.md |
| H-A11Y-1 | H1 — TalkBack traversal order in WorkoutSessionScreen [Guessing — 1900-line screen with overlays, banners, matrix pickers]. | Test: enable TalkBack, open an active session, swipe-right through the whole screen; verify order is header → current exercise → set rows → rest overlay, that the rest-timer overlay (singleton-flow-driven, per rest-timer history) announces its countdown updates via a live region rather than never/every-tick, and that ` … | domains/08-accessibility.md |
| H-A11Y-2 | H2 — Toast announcement actually fires [Likely it does — `role="status"` on a newly inserted node is announced by NVDA/VoiceOver in most engine versions, but insertion-with-role is the flakiest live-region pattern]. | Test: with VoiceOver + Safari and NVDA + Chrome, trigger `toast.success` and `toast.error` from the nutrition target save; confirm both are spoken without focus moving. If silent, mount a persistent `aria-live` container and inject text into it. | domains/08-accessibility.md |
| H-A11Y-3 | H3 — Focus trap vs. portalled/nested content [Likely fine]. | Test: open a modal containing a select and a date input, Tab from the last control (must wrap to first), Shift+Tab from first (must wrap to last), press Escape mid-text-selection (must close and restore focus to the trigger button). | domains/08-accessibility.md |
| H-A11Y-4 | H4 — 36dp IconButton effective touch target [Likely 48dp]. | Test: enable "Pointer location" in Android developer options, tap 6dp outside the visual undo button in an active workout; if the tap doesn't register, A11Y-008 upgrades to a real fix. | domains/08-accessibility.md |
| H-A11Y-5 | H5 — SettingsToggleRow TalkBack reading [Likely broken as described]. | Test: TalkBack on Settings; verify whether swiping reaches the label and switch as separate nodes and whether the switch announces its label or just "switch, on". Confirms A11Y-002's severity. | domains/08-accessibility.md |
| H-A11Y-6 | H6 — 200%/400% browser zoom on dashboard [Likely clipped]. | Test: Chrome, 1280px window, zoom 400%; verify no horizontal scrolling is required to read stat cards and nothing is clipped by the two `overflow-hidden` wrappers (`app/page.tsx:43,47`). | domains/08-accessibility.md |
| H-A11Y-7 | H7 — BloodPanel hidden chart has equivalent text [Likely partial]. | Test: VoiceOver over the dashboard blood panel; confirm every marker value drawn in the `aria-hidden` SVG is also announced from the visible marker rows. | domains/08-accessibility.md |
| H-OBS-1 | H1 — `System.Logger` output from the push package may bypass Logback formatting. `SyncChangePublisher`/`UserNotificationPusher`/`FcmSenderConfig` use `java.lang.System.Logger` (e.g. `UserNotificationPusher.java:31`) while the rest of the backend uses SLF4J; `backend/build.gradle.kts` has no `slf4j-jdk-platform-logging` (grep hit: none). Spring Boot's JUL→SLF4J bridge *usually* … | Method: run the backend locally and confirm a `UserNotificationPusher` warn appears in Logback format; or add `org.slf4j:slf4j-jdk-platform-logging` and standardize on SLF4J. | domains/09-observability.md |
| H-OBS-2 | H2 — Console-created billing budgets may exist at billing-account level (see OBS-010 null recording). | Method: `gcloud billing budgets list --billing-account=0008AE-458280-6056E2` with billing-scoped auth, or check the console. | domains/09-observability.md |
| H-OBS-3 | H3 — Cloud Run built-in 5xx metrics likely show the historical incidents and could power a zero-code alert today. | Method: Monitoring query on `run.googleapis.com/request_count` filtered `response_code_class="5xx"` for `health-fitness-backend` (not run — timeseries query beyond the agreed read set). | domains/09-observability.md |
| H-OBS-4 | H4 — Play Console vitals are the only existing Android crash signal and are likely not alert-configured. | Method: Play Console → Android vitals → set crash-rate email alerts (operator action; not verifiable from repo/ADC). | domains/09-observability.md |
| H-OBS-5 | H5 — Wear module has even less visibility than phone (no FCM token registration logging reviewed there; no crash reporting). | Method: repeat the OBS-002 dependency sweep on `android/wear*` module build files after Crashlytics decision. | domains/09-observability.md |
| H-TEST-1 | [Likely] The uat/ suite fails against HEAD (settings-UI testid overhaul #227 + new nutrition flows postdate its last commit). | Method to confirm: `bash infra/scripts/uat.sh` locally (not run — R3). | domains/10-testing.md |
| H-TEST-2 | [Likely] Cloud Build deploy triggers fire on push-to-main with no required-status check (operator memory + zero deploy config in repo); | confirm: GCP console trigger config / GitHub branch-protection API. | domains/10-testing.md |
| H-TEST-3 | [Guessing] Some @SpringBootTest medication/DEXA controller tests assert only HTTP status against the no-op stubs (TestPersistenceConfig.java:461-536) and could not fail on a dropped persistence call; | confirm: reading MedicationControllerTest assertions against stub wiring. | domains/10-testing.md |
| H-TEST-4 | [Guessing] Jacoco line coverage is moderate (50-70%) given 860 tests over ~200 controllers/services; | confirm: downloading the `backend-jacoco` artifact from any recent backend-ci run. | domains/10-testing.md |
| H-CICD-1 | H1: `tesseta-ci@health-fitness-160` may not exist or may lack deploy roles — I could not read IAM (did not attempt policy read; out of read-only comfort for this audit). If it exists with roles, CICD-006's re-import risk drops to Low. | (method embedded in hypothesis text) | domains/11-cicd.md |
| H-CICD-2 | H2: The versionCode fallback (CICD-003) may already have fired in an early build; if any distributed build shows a 7-digit build number in Firebase App Distribution history, current 634-range builds are *already* un-installable as updates for anyone on it. | (method embedded in hypothesis text) | domains/11-cicd.md |
| H-CICD-3 | H3: `web-ci` ~2 min per shared context vs the heavyweight step list (Playwright e2e, build, coverage — `web-ci.yml:36-78`) suggests either good caching or e2e running against a trivially small suite; no recent web-ci run appeared in the sampled window to verify. | (method embedded in hypothesis text) | domains/11-cicd.md |
| H-CICD-4 | H4: Cloud Build dollar cost — no rate could be fetched (pricing page content truncated); the monthly estimate is order-of-magnitude only. | (method embedded in hypothesis text) | domains/11-cicd.md |
| H-CICD-5 | H5: `terraform-ci` validates but nothing applies IaC automatically; live-vs-repo drift like CICD-006 is therefore likely present in other Terraform-managed resources too (D-infra overlap; not enumerated here). | (method embedded in hypothesis text) | domains/11-cicd.md |
| H-SOTA-1 | H1: The Tailwind and terraform-provider-google release dates returned by fetch summaries showed impossible years (2024) for versions newer than the repo's 2026 pins; version numbers are trusted, those two dates are low-confidence. | (method embedded in hypothesis text) | domains/12-sota-drift.md |
| H-SOTA-2 | H2: Wear OS app may be exempt-able from the 36 deadline if it's distributed only as a bundled companion (not independently updated) — verify in Play Console before spending effort on wear targetSdk. | (method embedded in hypothesis text) | domains/12-sota-drift.md |
| H-SOTA-3 | H3: `google-genai` 1.69.0 and `firestore` 3.46.0 backend clients likely have newer releases (not fetched this run — no currency claim made); check during MIG-003. | (method embedded in hypothesis text) | domains/12-sota-drift.md |
| H-SOTA-4 | H4: Play Console likely shows an active warning banner for the API-36 requirement with a one-click extension request; filing it buys until 2026-11-01 and should be done today regardless of MIG-001 timing. | (method embedded in hypothesis text) | domains/12-sota-drift.md |
| H-SOTA-5 | H5: vitest ^3.0.5 latest not fetched; probable major drift (v4?) — check during MIG-002. | (method embedded in hypothesis text) | domains/12-sota-drift.md |
| H-COST-1 | H1: The actual invoice. No billing export/BigQuery sink is configured (D9: only `_Required`/`_Default` log sinks; Budget API disabled), and the Billing API exposes account linkage, not line items. All dollar totals here are models; the ~$115–125/mo estimate could be off by ±30% (e.g., committed-use discounts, credits, or an account-level console budget I can't see). | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-2 | H2: Google Search grounding fee for `DrugLookupService` — priced separately per grounded request on the Gemini pricing page's tools section, which the fetch didn't return; unquantified. Low volume (drug adds), but nonzero. | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-3 | H3: Image input/output token counts assumed (~1.3k tok/photo in; image out priced at the page's own ≈$0.067/1K-image equivalence). Real capture photos at 2–4 MB may tokenize higher; batch API (50% off) is unused and is a further lever. | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-4 | H4: Actual current Gemini spend — modeled ~$8/user-mo assumes design usage; the Aug 2026 investigation (operator memory) found tesseta near-idle, so today's real number is likely $1–5/mo. | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-5 | H5: Withings API commercial terms at scale (free tier limits unknown); Google Health passive. | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-6 | H6: Free-tier applicability of Cloud Run's 240k vCPU-s to instance-based billing was taken from a search-verified summary of cloud.google.com/run/pricing, not the raw table — if it applies only to request-based, COST-001's number rises by ~$6/mo (direction favors the finding). | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-7 | H7: `thumbnail-fn` Cloud Functions invocation volume (assumed negligible; gcf-artifacts is only 102 MB). | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COST-8 | H8: Whether any android Cloud Build runs happen off-main (PR builds would multiply COST-004); the 12 most recent builds were all main-trigger deploy/release builds, suggesting not. --- | (method embedded in hypothesis text) | domains/13-cost.md |
| H-COMP-1 | H1 [Likely]: The `GEMINI_API_KEY` belongs to billing-linked project `health-fitness-160` (billing confirmed enabled by OBS live read), so paid-tier no-training terms govern and privacy.html §3 is true. | Method: AI Studio / Cloud console — confirm the key's project and that Gemini API usage bills to it; record the check date in the privacy doc. | domains/14-compliance.md |
| H-COMP-2 | H2 [Likely]: The Google OAuth consent screen is still in Testing (unverified from repo; operator memory of 7-day token expiry). | Method: Cloud console → OAuth consent screen status; determines COMP-003 lead time. | domains/14-compliance.md |
| H-COMP-3 | H3 [Guessing]: No signed/reviewed DPA posture exists beyond default Google terms (PD:95-96 admits review pending). | Method: confirm Google Cloud DPA auto-acceptance in the account, and read the Gemini API Additional Terms' processing commitments against PP §4's "under contract" wording. | domains/14-compliance.md |
| H-COMP-4 | H4 [Guessing]: `placeholder.tesseta.app` is unregistered/dead, making the Android privacy/terms links 404 or worse (a lapsed domain could be squatted and serve a fake policy). | Method: DNS lookup; regardless, fix per COMP-002. | domains/14-compliance.md |
| H-COMP-5 | H5 [Guessing]: Uploaded meal photos retain EXIF (incl. GPS) and sit on the public bucket, i.e., location data leakage beyond the health data itself. | Method: read the backend nutrition image upload path for EXIF stripping; fetch one object's bytes from a test account. | domains/14-compliance.md |
| H-DX-1 | H1: Branch protection does not require green CI before merge (would sharpen DX-002 from "no linkage" to "nothing gates an agent merge"). Not verifiable read-only from the repo; check GitHub settings or `gh api repos/:owner/:repo/branches/main/protection`. | (method embedded in hypothesis text) | domains/15-dx.md |
| H-DX-2 | H2: The `?? web/node_modules` symlink is created by hand per worktree (no script references it; `grep -rn "ln -s" scripts/ infra/scripts/` finds nothing) — | confirm: the operator; if so it belongs in the DX-007 bootstrap script. | domains/15-dx.md |
| H-DX-3 | H3: `/CLAUDE.md:32` says worktrees live in `.worktrees/` at the repo root, but this session's worktree is under `~/.anvil/worktrees/` — the harness convention may have drifted from the doc. Verify against the operator's current tooling before "fixing" either. | (method embedded in hypothesis text) | domains/15-dx.md |
| H-DX-4 | H4: Cloud Build per-trigger history would show how often `promote-canary` has failed silently (quantifying DX-001's null cost). | Method: ADC + Cloud Build REST `builds.list` filtered by trigger id and `status=FAILURE`. | domains/15-dx.md |
| H-PROD-1 | H1 [Likely]: The OAuth platform (ADR-0020) was built as portfolio/optionality, not demand-driven — zero registration seeds and admin-manual onboarding suggest no committed consumer. Mothballing behind a flag loses nothing. | (method embedded in hypothesis text) | domains/16-product-gaps.md |
| H-PROD-2 | H2 [Guessing]: Browser-on-iPhone is (or will be) the de-facto iOS strategy; if so, web photo capture (PROD-004) is quietly the highest-leverage parity item on this list. | (method embedded in hypothesis text) | domains/16-product-gaps.md |
| H-PROD-3 | H3 [Likely]: Fixing FCM (ops-only) + CTA empty states + barcode + PR card ≈ two weeks of work that moves the product from "impressive demo for one user" to "handable to a friend" — nothing on that path requires new architecture. | (method embedded in hypothesis text) | domains/16-product-gaps.md |
| H-PROD-4 | H4 [Guessing]: If coach/trainer sharing is ever the monetization path, the uid==self assumption is the single most expensive thing in the codebase to unwind; deciding this early is worth more than any feature on the backlog. | (method embedded in hypothesis text) | domains/16-product-gaps.md |
