# Local provider cache + state — never commit. The .terraform.lock.hcl (provider
# version/hash lock) IS committed, on purpose.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
override.tf
override.tf.json
crash.log
