<?xml version="1.0" encoding="utf-8"?>
<!--
    IMPL-AND-04: paths exposed through the app's FileProvider authority.
    Blood-test PDFs are cached under cacheDir/blood/ before being handed
    to the system PDF viewer via Intent.ACTION_VIEW.
-->
<paths>
    <cache-path name="blood_cache" path="blood/" />
</paths>
